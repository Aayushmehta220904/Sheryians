import { useState } from 'react'; import { ArrowRight, Eye, EyeOff, Lock, Mail, Sparkles, User } from 'lucide-react'; import { Link, useNavigate } from 'react-router'; import { useStore } from '../context/StoreContext'; import { toast } from 'react-toastify';
export default function Signup(){const {register}=useStore();const nav=useNavigate();const [show,setShow]=useState(false);const [f,setF]=useState({name:'',email:'',password:'',confirm:''});const submit=e=>{e.preventDefault();if(f.password.length<6)return toast.error('Password must be at least 6 characters');if(f.password!==f.confirm)return toast.error('Passwords do not match');if(!register({name:f.name,email:f.email,password:f.password}))return toast.error('Email already registered');toast.success('Account created');nav('/')};return <div className="auth-page signup"><section className="auth-showcase"><div className="brand"><span className="brand-mark"><Sparkles/></span><span>Sky<span>Mart</span></span></div><div><span className="eyebrow">Join the marketplace</span><h1>Your next great find starts here.</h1><p>Create one account for products, saved items and a persistent cart.</p></div><div className="metric-strip"><div><b>100+</b><span>Products</span></div><div><b>20+</b><span>Categories</span></div><div><b>24/7</b><span>Access</span></div></div></section><section className="auth-form-side"><form className="auth-card" onSubmit={submit}><span className="eyebrow">Create account</span><h2>Start shopping smarter</h2><label>Full name<div className="input-wrap"><User/><input required value={f.name} onChange={e=>setF({...f,name:e.target.value})} placeholder="Aayush Mehta"/></div></label><label>Email<div className="input-wrap"><Mail/><input type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})} placeholder="you@example.com"/></div></label><label>Password<div className="input-wrap"><Lock/><input type={show?'text':'password'} required value={f.password} onChange={e=>setF({...f,password:e.target.value})} placeholder="Minimum 6 characters"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><label>Confirm password<div className="input-wrap"><Lock/><input type={show?'text':'password'} required value={f.confirm} onChange={e=>setF({...f,confirm:e.target.value})} placeholder="Repeat password"/></div></label><button className="primary-btn">Create account <ArrowRight/></button><p className="switch-auth">Already registered? <Link to="/login">Sign in</Link></p></form></section></div>}
