import { useState } from 'react'; import { Eye, EyeOff, Lock, Mail, Sparkles, ArrowRight, ShieldCheck, Truck, BadgeIndianRupee } from 'lucide-react'; import { Link, useNavigate } from 'react-router'; import { useStore } from '../context/StoreContext'; import { toast } from 'react-toastify';
export default function Login(){const {login}=useStore();const nav=useNavigate();const [show,setShow]=useState(false);const [form,setForm]=useState({email:'',password:''});const submit=e=>{e.preventDefault();if(login(form.email,form.password)){toast.success('Welcome back');nav('/')}else toast.error('Invalid email or password')};return <div className="auth-page"><section className="auth-showcase"><div className="brand"><span className="brand-mark"><Sparkles size={18}/></span><span>Sky<span>Mart</span></span></div><div><span className="eyebrow">Smart shopping, simplified</span><h1>Everything worth buying, in one place.</h1><p>Discover trusted products, compare clearly and shop without the clutter.</p><div className="auth-benefits"><span><Truck/>Fast dispatch</span><span><ShieldCheck/>Secure checkout</span><span><BadgeIndianRupee/>Fair pricing</span></div></div><div className="quote-card">“A storefront designed around decisions, not distractions.”</div></section><section className="auth-form-side"><form className="auth-card" onSubmit={submit}><span className="eyebrow">Welcome back</span><h2>Sign in to SkyMart</h2><p>Use your account to continue shopping.</p><label>Email<div className="input-wrap"><Mail/><input type="email" required placeholder="you@example.com" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div></label><label>Password<div className="input-wrap"><Lock/><input type={show?'text':'password'} required placeholder="Enter password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><button className="primary-btn" type="submit">Sign in <ArrowRight/></button><div className="demo-note">Demo: demo@skymart.in / demo123</div><p className="switch-auth">New here? <Link to="/signup">Create an account</Link></p></form></section></div>}
