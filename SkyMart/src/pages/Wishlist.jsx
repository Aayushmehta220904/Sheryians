import { Heart } from 'lucide-react'; import { Link } from 'react-router'; import ProductCard from '../components/ProductCard'; import { useStore } from '../context/StoreContext';
export default function Wishlist(){const {wishlist}=useStore();return <section className="shop-page"><div className="page-hero small"><span className="eyebrow">Saved for later</span><h1>Your wishlist</h1><p>Keep products here until you are ready.</p></div>{wishlist.length?<div className="product-grid">{wishlist.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<div className="empty-state inline"><Heart/><h2>No saved products yet</h2><Link className="primary-btn" to="/shop">Browse products</Link></div>}</section>}
