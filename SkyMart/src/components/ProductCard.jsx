import { Heart, ShoppingBag, Star } from 'lucide-react'; import { Link } from 'react-router'; import { useStore } from '../context/StoreContext';
const money=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n*83);
export default function ProductCard({product}){const {addToCart,toggleWishlist,wishlist}=useStore();const liked=wishlist.some(i=>i.id===product.id);return <article className="product-card"><div className="product-image"><img src={product.thumbnail} alt={product.title}/><button className={liked?'wish active':'wish'} onClick={()=>toggleWishlist(product)}><Heart size={18} fill={liked?'currentColor':'none'}/></button><span className="discount">-{Math.round(product.discountPercentage||0)}%</span></div><div className="product-info"><p className="category">{product.category}</p><Link to={`/products/${product.id}`}><h3>{product.title}</h3></Link><div className="rating"><Star size={15} fill="currentColor"/>{Number(product.rating).toFixed(1)} <span>({product.stock} in stock)</span></div><div className="price-row"><strong>{money(product.price)}</strong><button onClick={()=>addToCart(product)}><ShoppingBag size={17}/> Add</button></div></div></article>}
